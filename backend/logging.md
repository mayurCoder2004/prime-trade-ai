# Logging (Optional Bonus)

- For production, use a logging library like `winston` or `pino` for structured logs.
- Example: Replace `console.log` and `console.error` with logger calls.
- Logs can be sent to files, cloud logging, or monitoring tools for scalability.
